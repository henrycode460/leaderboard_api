const nameValue = document.querySelector('.nameInput')
const scoreValue = document.querySelector('.scoreInput')
const detailDisplay = document.getElementById('displayList');
const inputForm = document.getElementById('formList');
const refershBtn = document.getElementById('refershBtn')










export{nameValue, scoreValue, detailDisplay, inputForm, refershBtn}